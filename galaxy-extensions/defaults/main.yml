galaxy_version: "{{ __galaxy_major_version | default('latest') }}"
galaxy_extensions_dir: '{{ galaxy_mutable_data_dir }}/webhooks'
galaxy_extensions_enabled: []
  # - example_extension_id

# Don't remove these extensions from the extensions dir
galaxy_default_extensions:
  - demo
  - gtn
  - news
